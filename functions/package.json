{
  "name": "afc-neighborhood-watch-functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "csv-parse": "^5.6.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0",
    "nodemailer": "^8.0.5"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0",
    "typescript": "~5.7.0",
    "vitest": "^4.1.4"
  }
}
